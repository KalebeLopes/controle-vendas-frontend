export class User {
  constructor(
    public nome: string, 
    public email: string,
    public senha: string
  ) {}
  
  // public aplicarDesconto = (preco: number): number => {
  //   return preco - this.desconto
  // }

}